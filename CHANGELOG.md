# 更新日志 (Changelog)

本项目所有的显著更改都将记录在这个文件中。

本项目遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/) 规范。

## [1.1.0] - 2026-03-29

### 新增 (Added)
- 🚀 **全面支持声调解析**：新增 `pinyinTone()` 和 `nameTone()` 方法，原生输出带注音标的汉语拼音，满足前端注音与教育展示需求。
- 📚 **字典核弹级扩容**：弃用 `.dat` 文本，引入 `pinyin_dict.php` 数组字典，收录多达 **20,857** 个汉字（覆盖几乎所有 CJK 统一表意文字及扩展 A 区）。
- ⚡ **无损降级映射表**：内置 `TONE_MAP` 算法，实现带有声调的生僻发音（如 ḿ, ń, ế 等成音节辅音）向标准 a-z 的优雅降级转换，彻底杜绝乱码。

### 更改 (Changed)
- ⚙️ **OPcache 性能红利**：底层字典加载机制改为原生 `require` PHP 数组文件。在开启 OPcache 的生产环境中，将体验到 0 毫秒级别的 I/O 解析损耗。
- 🛡️ 保持了 100% 的向下兼容：原有的 `pinyin()`, `slug()`, `abbr()`, `name()` 方法行为不变（默认自动过滤声调）。

## [1.0.1] - 2026-03-28

### 修复 (Fixed)
- 🐛 勘误 `data/pinyin-utf8.dat` 数据文件，修复了部分常见汉字与生僻字的拼音映射错误，提升转换准确率。

## [1.0.0] - 2026-03-28

### 新增 (Added)
- 🚀 **全新现代化架构**：全面支持 PHP 7.4+，引入严格的强类型声明与返回类型提示。
- ⚡ **极致性能引擎**：底层引入基于 `\p{Han}` 的 PCRE 正则智能分词引擎，彻底解决原版 `mb_substr` 循环遍历带来的性能瓶颈。
- 🧠 **内存级字典缓存**：采用单例模式加载 `pinyin-utf8.dat` 纯 UTF-8 字典，确保单次请求生命周期内零冗余 I/O 消耗。
- 🛠 **极简 API**：提供 4 个开箱即用的核心方法：
  - `pinyin()`：基础全拼转换。
  - `slug()`：生成对 SEO 完美友好的安全 URL 别名。
  - `abbr()`：提取拼音首字母缩写。
  - `name()`：拼音首字母自动大写，专供标题与人名使用。
- 🌐 **命名空间与自动加载**：引入 `Zidon\PhpPinyin` 命名空间，完美契合 PSR-4 标准，零摩擦接入 CodeIgniter 4、Laravel 等现代框架。

### 修复 (Fixed)
- 🐛 修复了由于标点符号未过滤导致的拼音连接符 (`-`) 重复拼装的 Bug。
- 🐛 修正了输入生僻字时可能触发系统警告的问题，增加了安全的回退机制。